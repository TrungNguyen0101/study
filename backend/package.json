{
  "name": "vocabulary-backend",
  "version": "1.0.0",
  "description": "Backend for vocabulary learning app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build step required'",
    "dev": "nodemon server.js --watch . --ext js,json --ignore node_modules/",
    "watch": "nodemon server.js --watch . --ext js,json --ignore node_modules/"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
